@startuml
!include style.iuml
title **IModbusElementWriter**

start

-newModbusData(data)

while (running==<color:blue>true</color>)
    if (conditionsMet(data)) then (<color:green>yes</color>)
        -device.write(captured)\n\nrunning = false
    else (<color:red>no</color>)
    endif
endwhile (stop)

end

@enduml
